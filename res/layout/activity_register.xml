<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/main_back_ground"
    tools:context=".LoginActivity" >

    <include
        android:id="@+id/register_title_layout"
        layout="@layout/activity_title_layout" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/register_title_layout"
        android:orientation="vertical" >

        <include
            android:id="@+id/register_main_view1"
            layout="@layout/activity_register1" >
        </include>

        <include
            android:id="@+id/register_main_view2"
            layout="@layout/activity_register2"
            android:visibility="gone" >
        </include>
    </LinearLayout>

</RelativeLayout>